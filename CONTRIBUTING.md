# コントリビュート

## ライセンスと出所

本リポジトリは **MIT** です。パッチも同じ MIT で許諾されます。
CLA はありません。代わりに **DCO**（Developer Certificate of Origin）を使います。

コミットに署名行を付けてください。

```bash
git commit -s -m "..."
```

これは <https://developercertificate.org/> の内容に同意したことを意味します。

## 特許

**MIT には特許条項がありません。** このソフトウェアの利用者に対して、
こちらから特許を許諾することはありません。

一方で、**寄与に含まれる特許については、寄与者から許諾を受けます。**
これが無いと、寄与を受け入れた後に、その寄与者から特許を主張されうるためです。

前節の署名行（`git commit -s`）は、DCO への同意に加えて、次に同意したことを
意味します。

1. **通常実施権の許諾** — 自分が保有または管理する特許のうち、その寄与を
   実施するために必要なものについて、本プロジェクトおよび本ソフトウェアの
   利用者へ、**無償・非独占・世界的・取消不能の通常実施権**を許諾する
2. **開示の義務** — その寄与に関係する特許（自分のものか第三者のものかを
   問わない）を知っている場合は、**プルリクエストに明記して通知する**
3. **通知を怠った場合** — 開示しなかったとき、または通知を怠ったときは、
   **1 の許諾をしたものとみなす**

関係する特許があるなら、コードを送る前に issue で相談してください。
早い段階なら、設計を変えて回避できることがあります。

## 変更を送る前に

```bash
cargo fmt --all -- --check
cargo clippy --all-targets -- -D warnings
cargo test
```

3 つとも通ることを確認してください。**このリポジトリに CI はありません。**
検査は各自の手元で回します。

## 設計上の約束

壊すと直しにくいものが 2 つあります。変更する場合は理由を PR に書いてください。

- **発話の区切りを決めるのは 1 か所だけ。** クライアントとサーバーの両方に
  終話判定を持たせない。両方が判定すると、片方が「発話は終わった」と
  見なした瞬間に音声の蓄積が止まり、もう片方は空の結果を受け取る
- **マイクが動いている間、音声を捨てない。** 接続を張り直している最中でも
  リングバッファに積む。捨てると次の発話の先頭が欠ける

## コミットメッセージ

1 行目は `種別(範囲): 要約`。本文には「何を変えたか」ではなく
**「なぜその変更が必要か」** と、**壊れていたときに何が起きるか** を書いてください。
